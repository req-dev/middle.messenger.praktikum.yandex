.form-common_disabled{
    opacity: 0.4;
}

.form-common__error-message{
    color: var(--color-red);
    font-family: Inter, sans-serif;
    font-size: 11px;
    font-weight: 500;
    line-height: 100%;
}
